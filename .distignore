# Pliki wykluczone z ZIP dystrybucyjnego

# Dev
tests/
scripts/
CLAUDE.md
CHANGELOG.md
cliff.toml
.wp-env.json
phpunit.xml
phpstan.neon
.phpcs.xml
coverage/

# Zależności dev (vendor budowany z --no-dev)
# vendor/ jest WŁĄCZONY do ZIP

# Config
package.json
package-lock.json
node_modules/
composer.json
composer.lock

# CI / VCS
.git/
.github/
.gitignore
.gitattributes
.distignore

# Build artefakty
dist/
coverage/
.phpunit.result.cache

# Inne repo-specyficzne
supabase/
.claude/
README.md
